{
  "name": "rasterized-pdfs-poc",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "esbuild index.js --sourcemap --bundle --outdir=build --servedir=.",
    "build": "esbuild index.js --minify --bundle --outdir=build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.19.5"
  },
  "dependencies": {
    "jspdf": "^2.5.1",
    "pdfjs-dist": "^3.11.174"
  }
}

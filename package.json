{
  "name": "rasterized-pdfs-poc",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "esbuild index.js main.css --sourcemap --bundle --outdir=build --servedir=.",
    "build": "esbuild index.js main.css --minify --bundle --outdir=build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.19.5"
  },
  "dependencies": {
    "bulma": "^0.9.4",
    "jspdf": "^2.5.1",
    "pdfjs-dist": "^3.11.174"
  },
  "volta": {
    "node": "20.17.0"
  }
}
